Task ID: T101

You are the Common-Infrastructure-Agent. Your worktree is /home/hungnguyenhuu/workspace/pets/wt-t101 and your branch is
fix/t101-docs-reconcile. Work only inside that worktree.

Read these three in full before doing anything else:
  1. /home/hungnguyenhuu/workspace/pets/wt-t101/tasks/TASK_GUIDE_T101.md  — the contract. Read it completely.
  2. /home/hungnguyenhuu/workspace/pets/wt-t101/agents/common-infrastructure.md — your role guide.
  3. /home/hungnguyenhuu/workspace/pets/wt-t101/memory/MEMORY.md — read it in full at this path. It is not pasted here.

Restated intent, verbatim from the guide:
> main..v2 is 90 commits (T094-T100) and main is 0 behind. Three of those tasks changed
> user-facing behaviour. The Install and Update-flow sections of the site already document T097
> correctly. Four other places still describe the pre-T096/T097 world and are now factually wrong
> or materially incomplete. Correct exactly those four, and add drift tests so the same four
> cannot silently re-rot - tests/test_site_content.py did not catch any of them.

Two scope locks were chosen by the user at Stage 2. Do not re-litigate either, and do not "fix"
them while you are in the file: the curl install URL stays on /main/setup.sh, and the site footer
stays "v1 release". AC8 tests that both are byte-unchanged.

**Demonstration BEFORE capture**: before your first implementation commit, fill the
## Demonstration BEFORE field in /home/hungnguyenhuu/workspace/pets/wt-t101/tasks/TASK_REVIEW_T101.md (the guide carries only a
"> **Moved.**" pointer). This task does not change executable code, so BEFORE is the verbatim
prior content of each thing you are about to change, quoted from the file as it exists right now
- the two .claude/* rows of the layout table, the Options table as it stands, the two Providers
sentences, and README.md line 11. A BEFORE captured after the change is not a BEFORE.

**Trace attribution**: before running any test or verification command, write the active-task
state file so the trace hook can attribute your Bash calls:
  mkdir -p /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state && printf '%s\n%s\n' "T101" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state/active_task
Do this once at the start. Do NOT try CLAUDE_ACTIVE_TASK=T101 <cmd> inside a Bash tool call, and
do NOT write a cwd-relative path - neither reaches the hook.

Record the full-suite baseline count BEFORE you change anything. Write the three new tests first
and watch them fail against the current page. M1 and M2 in the guide are mandatory mutation
controls - a new test never observed RED is not evidence in this repo.

When done, report status to the Supervisor with Task ID T101.
