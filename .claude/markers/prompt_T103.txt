Task ID: T103

You are the Common-Infrastructure-Agent. Your worktree is /home/hungnguyenhuu/workspace/pets/wt-t103 on branch
fix/t103-response-standard-channel. Work only inside that worktree.

Read in full before anything else:
  1. /home/hungnguyenhuu/workspace/pets/wt-t103/tasks/TASK_GUIDE_T103.md — the contract
  2. /home/hungnguyenhuu/workspace/pets/wt-t103/agents/common-infrastructure.md — your role guide
  3. /home/hungnguyenhuu/workspace/pets/wt-t103/memory/MEMORY.md — read at this path; not pasted here
  4. The entry "A pointer in CLAUDE.md to a non-injected file does not bind the Supervisor" in
     /home/hungnguyenhuu/workspace/pets/wt-t103/memory/learnings.md — the measured account of the defect

Restated intent, verbatim from the guide:
> T100's six Response Standard rules are correct and stay exactly as written. Their delivery
> channel to the Supervisor is what failed. CLAUDE.md currently says "agents/general-agent-
> template.md's ## Response Standard binds the Supervisor too: read it and apply it to your own
> replies" - a rule whose activation depends on the Supervisor performing an unenforced act
> (opening a file the harness does not auto-inject). Move the rules themselves into CLAUDE.md,
> which IS auto-injected, and pin that with a test so the next edit cannot silently revert it to
> a pointer.

Do not reword any of the six rules. Do not touch agents/general-agent-template.md or the four role
guides - sub-agents already receive the standard correctly and AC3/AC9 pin that.

**Order of operations matters.** Editing CLAUDE.md makes test_agent_guide_dedup.py's AC5 pin red by
construction. Commit the CLAUDE.md change FIRST, then repoint T070_BASELINE_REF to that commit's
hash in a SECOND commit, with a comment saying what changed and why - the convention T071, T082,
T096 and T100 each followed. Repoint, never delete; do not touch the assertion body.

**Demonstration BEFORE capture**: before your first implementation commit, fill the ## Demonstration
BEFORE field in /home/hungnguyenhuu/workspace/pets/wt-t103/tasks/TASK_REVIEW_T103.md. This task changes no executable code, so BEFORE is
the verbatim current text of CLAUDE.md's "## Supervisor Communication Style" opening paragraph,
quoted from the file as it exists right now.

**Trace attribution**: before running any test, write the active-task state file:
  mkdir -p /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state && printf '%s\n%s\n' "T103" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state/active_task

Record the full-suite baseline count BEFORE changing anything. Six tests currently fail on v2
(MEMORY.md hot-tier budget and README line count) - they are pre-existing, tracked as T102, and are
NOT yours to fix. Confirm they are unchanged in name and count at the end; do not touch
memory/MEMORY.md or README.md.

Write the tests first and watch them fail. Then run all three mutation controls M1, M2 and M3 from
the guide and paste every RED transition and restore into the review file. M2 and M3 are not
optional extras: a test that only checks CLAUDE.md contains the rules would stay green on a repo
where the two copies had drifted apart, or where the template had lost the section entirely and
sub-agents were silently broken. Pin all three directions.

Report status to the Supervisor with Task ID T103.
