Task ID: T102

You are the Common-Infrastructure-Agent. Your worktree is /home/hungnguyenhuu/workspace/pets/wt-t102 on branch
fix/t102-baseline-green. Work only inside that worktree.

Read in full before anything else:
  1. /home/hungnguyenhuu/workspace/pets/wt-t102/tasks/TASK_GUIDE_T102.md — the contract
  2. /home/hungnguyenhuu/workspace/pets/wt-t102/agents/common-infrastructure.md — your role guide
  3. /home/hungnguyenhuu/workspace/pets/wt-t102/memory/MEMORY.md — read at this path; not pasted here

Restated intent, verbatim from the guide:
> Make the v2 baseline actually green and make every place that describes that baseline — the
> handoff note and the site footer — state what is true, so the next session's baseline check is
> against a board that is not lying to it.

**Demonstration BEFORE capture**: before your first implementation commit, fill the ## Demonstration
BEFORE field in /home/hungnguyenhuu/workspace/pets/wt-t102/tasks/TASK_REVIEW_T102.md (the guide
carries only a "> **Moved.**" pointer). This task changes test constants and documentation text, so
BEFORE is (i) the real, timestamped output of
`python3 -m pytest tests/ .claude/hooks/tests/ -q 2>&1 | tail -3` run right now, before any edit,
and (ii) the verbatim current text of the site footer sentence and the KANBAN handoff note's
"clean and green" line, quoted from the files as they exist right now. A BEFORE captured after the
change is not a BEFORE; there is no N/A path.

**Trace attribution**: before running any test or verification command, write the active-task state
file so the trace hook can attribute your Bash calls:
  mkdir -p /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state && printf '%s\n%s\n' "T102" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state/active_task
Do NOT use CLAUDE_ACTIVE_TASK=T102 <cmd>, and do NOT write a cwd-relative path — neither reaches the
hook, and both leave your evidence unattributed.

Three things are decided and are NOT yours to relitigate:

1. **The README cap goes to 75. README.md itself is not touched.** The user chose this over
   re-slimming. T097's --harness block is a real feature earning real space, so 60 was the stale
   value, not the content. `git diff --name-only` must not list README.md. The test's **name** is
   part of the fix — a test called test_readme_is_at_most_60_lines that asserts 75 is exactly the
   name-misstates-the-fact defect half (c) exists to correct.

2. **Do not touch memory/MEMORY.md.** The 5 hot-tier budget failures are half (a); they are fixed
   by the user running /compact-memory, because compact-memory's own rule is "move syntheses down,
   never shorten them" and the largest index entries hold tallies existing nowhere else. If those
   5 are the only failures left at the end, report **blocked on the user**, never a pass, and never
   by shrinking the file to fit.

3. **The v1 release footer string and the /main/setup.sh install URL stay byte-unchanged.** Those
   are T101's AC8 scope locks and are still in force. You change two directory names inside the
   footer sentence and nothing else on that page.

Record the full-suite baseline count BEFORE changing anything: expect `6 failed, 837 passed`.

Write the tests first and watch them fail. Then run all three mutation controls M1, M2 and M3 from
the guide's Success Criteria and paste every RED transition and restore into the review file.
**M3 is the load-bearing one and is not optional**: change AGENTS_DIR in tests/test_site_content.py
to ROOT/.claude/agents and confirm the new footer test goes RED *without the footer changing*. M2
alone stays green on a test that hardcodes the literal "agents/"; only M3 proves the assertion is
derived from source at test time. A test never observed RED is not evidence.

Report status to the Supervisor with Task ID T102.
