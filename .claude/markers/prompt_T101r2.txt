Task ID: T101 — ROUND 2 (Stage 4 review fixes)

Your round-1 work was reviewed and PASSED at 0 P0 / 0 P1. The four content corrections and all
three drift tests are accepted and must NOT be changed. This round fixes test-quality findings
only. Do not touch site/index.html content, README.md, or any Acceptance Criterion.

Read /home/hungnguyenhuu/workspace/pets/wt-t101/tasks/TASK_REVIEW_T101.md first — the Stage 4 section at the bottom states each finding
in full, with the evidence behind it. Then read /home/hungnguyenhuu/workspace/pets/wt-t101/tasks/TASK_GUIDE_T101.md for the contract.

Fix exactly these three, all in /home/hungnguyenhuu/workspace/pets/wt-t101/tests/test_site_content.py:

P2-1 (the one that matters). In test_providers_section_names_codex_skill_cap_and_skipped_skills,
  the line   assert "no Skill tooling" not in body and "no skill tooling" not in body.lower()
  is VACUOUS. The stale v2 page never contained that string — it said
  "cannot enforce: ... <code>Skill</code>/<code>Agent</code> tooling". Verify that yourself against
  git show v2:site/index.html before you start; do not take my word for it. Replace it with a
  STRUCTURAL assertion on the unenforceable-list: that the list no longer contains a
  <code>Skill</code> element while it DOES still contain <code>Agent</code> (the Agent spawn tool
  genuinely is still Claude-only, so that half must stay pinned too).
  Then MUTATION-PROVE it: restore the v2 wording of that sentence, confirm the assertion goes RED
  naming the right thing, restore, confirm GREEN. Paste both transitions into the review file. An
  assertion never observed RED is not evidence in this repo — that is exactly the defect you are
  fixing, so do not reintroduce it one level down.

P3-1. _skills_over_codex_cap() re-derives os.path.join(ROOT, "skills") although the module already
  defines SKILLS_DIR at line 20. Use the existing constant.

P3-2. The cap assertion checks the number and the unit independently:
  _word_present(body, f"{cap_kb}") and "KB" in body — so "8 skills" plus a "KB" elsewhere would
  pass. Assert the adjacency instead (e.g. a regex for the number followed by optional whitespace
  then KB). Latent today, not live: there is currently exactly one standalone 8 and one KB in that
  section. Fix it anyway.

Then: re-run the full suite (python3 -m pytest tests/ .claude/hooks/tests/ -q) and confirm the
failure set is still the SAME 6 pre-existing failures — 834 passed, 6 failed. Those 6 are
MEMORY.md-budget and README-line-count failures that were already red on v2 before this task. They
are NOT yours to fix and are being tracked as a separate task. Do not touch memory/MEMORY.md or
README.md line count to make them green.

**Trace attribution**: before running any test, write the active-task state file:
  mkdir -p /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state && printf '%s\n%s\n' "T101" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state/active_task

Append a "## Round 2" section to /home/hungnguyenhuu/workspace/pets/wt-t101/tasks/TASK_REVIEW_T101.md with the mutation transitions, then
commit. Report status to the Supervisor with Task ID T101.
