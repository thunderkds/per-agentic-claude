Task ID: T104

You are the Common-Infrastructure-Agent. Your worktree is /home/hungnguyenhuu/workspace/pets/wt-t104 on branch
fix/t104-site-version. Work only inside that worktree.

Read in full before anything else:
  1. /home/hungnguyenhuu/workspace/pets/wt-t104/tasks/TASK_GUIDE_T104.md — the contract
  2. /home/hungnguyenhuu/workspace/pets/wt-t104/agents/common-infrastructure.md — your role guide
  3. /home/hungnguyenhuu/workspace/pets/wt-t104/memory/MEMORY.md — read at this path; not pasted here

Restated intent, verbatim from the guide:
> The public page must name the release it actually ships with, and a test must derive that version
> from the release record so the next release cannot leave the page behind again.

This is C0. Two string edits and one new test. Resist every urge to improve anything adjacent.

**Demonstration BEFORE capture**: before your first implementation commit, fill the ## Demonstration
BEFORE field in /home/hungnguyenhuu/workspace/pets/wt-t104/tasks/TASK_REVIEW_T104.md with the
verbatim current text of site/index.html lines 180 and 562, quoted as they exist right now, plus the
real timestamped output of `python3 -m pytest tests/ .claude/hooks/tests/ -q 2>&1 | tail -3`
(expect 844 passed). A BEFORE captured after the change is not a BEFORE.

**Trace attribution**: before running any test, write the active-task state file:
  mkdir -p /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state && printf '%s\n%s\n' "T104" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > /home/hungnguyenhuu/workspace/pets/personal-agentic-claude/.claude/hooks/.state/active_task
Do NOT use CLAUDE_ACTIVE_TASK=T104 <cmd> and do NOT write a cwd-relative path — neither reaches the hook.

**AC3 is the whole task.** The two string edits are trivial and would stay correct on their own. The
test is what stops a fourth occurrence of "a doc surface asserts something that quietly stopped being
true" — T101 found four such places, T102 found the handoff note, this is the third in three tasks.
So the expected version MUST be parsed from RUNBOOK.md's Release Log table at test time. A hardcoded
"v2.0.0" literal fails this task even though it would pass on today's page.

**Run both mutation controls and paste every RED transition and restore into the review file.**
M1 is load-bearing and is not optional: append a `v2.1.0` row to RUNBOOK.md's Release Log, leave the
page untouched, and confirm the test goes RED naming v2.1.0 — then revert. M2 alone (revert a page
string) stays green on a test that hardcodes the version; only M1 proves the value is parsed. A test
never observed RED is not evidence. In T102 this exact control was lost when a mutation-revert
restored a backup taken before the fix existed, and the vacuous version shipped — check your restore.

**RUNBOOK.md is Must-NOT-Touch** outside M1's mutate-and-revert. It is the source of truth your test
reads; editing it to make a test pass is the breach that got reverted in T102.

**This task has a UI component**, so all three UI Evidence rows in the review file must be filled
with real evidence before it can be called done — not N/A. `v2.0.0` is two characters longer than
`v1`; check the sidebar badge and footer at 375px and 1280px in headless Chrome and paste what you
observed. google-chrome is installed.

Report status to the Supervisor with Task ID T104.
