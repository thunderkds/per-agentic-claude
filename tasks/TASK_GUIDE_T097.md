# TASK_GUIDE — T097 (B2): Per-harness install projection, `setup.sh --harness`, and the `AGENTS.md` correction that unblocks Codex
**Date**: 2026-08-27
**Complexity Level**: C2
**Risk Level**: Medium
**Priority**: P1
**Assigned agent**: Common-Infrastructure-Agent
**Agent guide**: `.claude/agents/common-infrastructure.md`

---

## Mandatory Startup (Do Not Skip)

Before writing any code:
1. Read `PROJECT_SPEC.md`
2. Read `memory/MEMORY.md`
3. Read this file completely
4. Read `.claude/agents/common-infrastructure.md`
5. C2 task, multi-file: read `memory/codebase-map.md` (regenerated by `T096`)
6. Read `docs/ddr/0007-canonical-skills-and-agents-at-plain-root.md` **in full**
7. Read `docs/adr/0001-direct-repo-install-no-central-clone.md` — the copy-not-symlink rule for
   downstream installs, and the `.claude/harness-lock.json` hash model this task must not break
8. Read `docs/ddr/0006-provider-adapters-inline-non-negotiables.md` — it stays in force; this task
   corrects one factual claim inside `AGENTS.md`, it does not change the adapter architecture

---

## Requirement (Pillar 1 — Adapt the requirement)

User request, 2026-08-27:

> "go with option B" … "generate plan for now, I will implement later"

with the harness set answered separately: **Claude Code + Codex only (N=2)**.

This is **B2**. `T096` (B1) moved canon to plain root. B2 makes it reach a second harness.

**Restated intent**:
> Make a Codex session in a project installed by this kit actually find and run the kit's skills, and
> stop the kit's own doctrine from claiming that is impossible. Harnesses are selected at install
> time so a project only ever receives directories for CLIs it actually uses.

### The user's original symptom, which this task closes

> "the skill is not available cause diff of folder name, path does not found"

Two causes, both addressed here:
1. Nothing ever wrote the kit's skills into a directory Codex reads.
2. `AGENTS.md:29-33` told the user and every agent that Codex has no skills at all, so nobody tried.

### Defect A — `AGENTS.md` states a falsehood about Codex

Current text, lines 29-33:

> "Codex has no equivalent of Claude Code's hooks, skills, or `Skill`/`Agent` tooling. It cannot run
> `code-review`, `security-review`, `verify`, `ship`, or `migration-safety`, and it does not get the
> git-guardrails PreToolUse hook."

The second sentence is **true and must survive**. The first is **half false**: Codex gained SKILL.md
support in Dec 2025 — `.codex/skills/` at project scope, `~/.codex/skills/` personal, with an **8 KB
skill-body cap**. Hooks and the `Skill`/`Agent` tooling remain genuinely unportable.

Rewrite to state precisely what Codex does and does not have. Do not overcorrect into implying the
pipeline runs there — that would reproduce T085's "looks live and is not" defect in the other
direction.

### Defect B — no projection mechanism exists

`install_abs` (`setup.sh:154`) does symlink-or-copy, but its header comment forbids reuse:
*"do not repurpose for the base install, which always copies via `harness_copy_manifest`."* It is
used only by `install_pack`. **The base install has no projection step — this is new code, not a
reuse.** (An earlier Supervisor claim that the mechanism already existed was wrong; it is corrected
here so the implementer does not go looking for it.)

### What ships

- `MANIFEST` gains a **destination map**: which canonical path lands in which directory per harness.
  Keep the existing one-path-per-line format working unchanged for the Claude default; a project that
  never passes `--harness` must behave exactly as it does today.
- `setup.sh --harness <name>` (repeatable) and the same flag on `update.sh`. Default with no flag:
  Claude only, i.e. today's behaviour.
- Codex destination: `skills/` → `.codex/skills/`, `agents/` → `.codex/agents/` (verify the agents
  path against Codex's current docs at implementation time; if Codex has no agents concept, ship
  skills only and say so rather than inventing a directory).
- Generated vendor directories are **gitignored** downstream. Canon is committed; projections are not.
- `AGENTS.md` corrected per Defect A, with an accurate Codex limits section.
- A Codex-specific limits statement naming what cannot run there: hooks, `code-review`,
  `security-review`, `verify`, `ship`, `migration-safety`.

### The 8 KB rule (load-bearing, from `DDR-0007`)

Codex caps a skill body at 8 KB. Several skills in this repo exceed that.

**On an oversize skill, fail loudly. Never truncate.** A silently trimmed skill looks installed and
behaves worse, which is strictly worse than "skill not found" because it is invisible. The install
must name the offending skill and its size, and either abort or skip-with-a-loud-warning — that
choice is the implementer's, but silence is not an option. This is the single rule carried forward
from the deferred Option C.

**Out of scope**:
- Cursor, Gemini, OpenCode, Copilot, Antigravity. N=2 was chosen deliberately: `DDR-0006`'s follow-up
  says *"at N=4 the generator's cost/benefit inverts and this DDR should be reconsidered rather than
  assumed."* Adding a third or fourth harness here would silently cross that line. Cursor keeps its
  existing `DDR-0006` doctrine-only stub.
- Content transformation per harness (`tools/adapters/`, Option C) — deferred until a real format
  divergence is measured. The 8 KB rule above is the one exception, and it is a *check*, not a
  transformation.
- Relocating `.claude/hooks/` or `.claude/settings.json`.
- `packs/` and `install_pack()`.
- Any change to `DDR-0006`'s adapter architecture. One factual sentence in `AGENTS.md` changes; the
  design does not.

**Requirement Refs**: `DDR-0007` (Decision, Follow-up) · `BRAINSTORMING_LOG_harness-kit-portability.md`
(Option B / B2, Edge Case Checklist)

### Requirement Fidelity Gate (sign off BEFORE implementation)

- [x] Restated intent confirmed to match the user's selection and the N=2 answer (Supervisor, 2026-08-27)
- [x] Domain terms align with `PROJECT_SPEC.md` — "harness", "projection", "destination map", "canon"
- [x] Every Acceptance Criterion below traces to a line in the Requirement or to `DDR-0007`
- [x] Requirement Refs resolve: `DDR-0007` and the brainstorming log exist on disk

---

## Dependencies & Reachability

**Depends on**: `T096` — canon must be at its final location before anything projects from it.

**Entry point**: `bash setup.sh --harness codex` in a throwaway git repo

---

## Acceptance Criteria

| # | Criterion (testable) | Traces to |
|---|----------------------|-----------|
| 1 | `setup.sh` with **no** `--harness` flag produces byte-identical output to today's install — the default path is unchanged | "must behave exactly as it does today" |
| 2 | `setup.sh --harness codex` in a fresh git repo produces `.codex/skills/` containing every skill under the cap, as real file copies (not symlinks) | Decision + `ADR-0001` |
| 3 | A **real Codex session** in that project finds a kit skill by name. Evidence is the session's own output, not a directory listing | "actually find and run" |
| 4 | A skill whose body exceeds 8 KB causes a **loud, named failure or warning** identifying the skill and its size; it is never silently truncated | The 8 KB rule |
| 5 | Negative: with the size check disabled, an oversize skill installs silently — proving AC4's check is load-bearing | anti-vacuity |
| 6 | `--harness` with an unknown name exits non-zero with a clear error naming the valid values; it does not produce a silently empty install | Edge Case Checklist |
| 7 | `setup.sh --harness codex` run **twice** is idempotent — no duplicated, half-written, or orphaned files | Edge Case Checklist |
| 8 | Generated vendor directories are gitignored; a fresh clone of a downstream project still installs cleanly | Decision |
| 9 | `update.sh` across the relocation does not leave two live sets of skills. Whichever behaviour ships (migrate / refuse / warn), it is **stated explicitly** and covered by a test — silent orphaning is a failure | `DDR-0007` Follow-up |
| 10 | `.claude/harness-lock.json` hash comparison still works against relocated paths — a stale lock is detected, not silently mismatched on every file | Edge Case Checklist |
| 11 | `AGENTS.md` no longer claims Codex has no skills; it names `.codex/skills/`, the 8 KB cap, and keeps the true sentence about hooks, `code-review`, `security-review`, `verify`, `ship`, `migration-safety` | Defect A |
| 12 | `tests/test_provider_adapters.py` stays green (11 passed) — `DDR-0006`'s byte-identical shared spans still hold after the `AGENTS.md` edit | Out of scope: `DDR-0006` unchanged |
| 13 | `python3 -m pytest .claude/hooks/tests/ -q` ≥ 697 passed, `python3 -m pytest tests/ -q` 40 passed, `bash scripts/smoke-install.sh` exits 0 | no regression |
| 14 | At least one new automated test per defect (A and B), plus AC4, AC6, and AC7, against constructed fixtures | Hard-Stop Gate 5 |

---

## Evaluation & Acceptance

### Success Criteria (observable, pass/fail)

| # | Given (input/state) | Expect (output/behavior) | How it's checked |
|---|---------------------|--------------------------|------------------|
| 1 | Throwaway git repo, `bash setup.sh` (no flag) | identical tree to today's install | automated test |
| 2 | Same repo, `bash setup.sh --harness codex` | `.codex/skills/` populated with real copies | automated test |
| 3 | Codex launched in that repo, skill invoked by name | Codex's own output shows the skill running | **manual probe, transcript pasted** |
| 4 | A fixture skill of 9 KB | named, loud failure or warning citing the size | automated test |
| 5 | Size check disabled, same fixture | installs silently | automated test (anti-vacuity probe) |
| 6 | `--harness banana` | non-zero exit, error naming valid values | automated test |
| 7 | `--harness codex` twice | second run changes nothing | automated test |
| 8 | `update.sh` run on a pre-relocation install | documented behaviour, no two live skill sets | automated test |

### Verification Command (exact, runnable)

```bash
python3 -m pytest .claude/hooks/tests/ -q && \
python3 -m pytest tests/ -q && \
python3 -m pytest tests/test_provider_adapters.py -q && \
bash scripts/smoke-install.sh
```

Expected: `≥697 passed`, `40 passed`, `11 passed`, exit 0.

### Evidence (filled by reviewer at Stage 4/5)

> Filled by the reviewer at Stage 4/5 in `tasks/TASK_REVIEW_T097.md`.

---

## Demonstration

> See `tasks/TASK_REVIEW_T097.md`.

---

## Approach

**Vital slice**: `--harness codex` producing a working `.codex/skills/`, plus the `AGENTS.md`
correction. Those two together are what turns the user's reported symptom from true to false.

**Cut list**:
- No Cursor, Gemini, or other harness. N=2 by decision.
- No content transformation per harness. The 8 KB check is a check, not a transform.
- No `npx`-style installer or marketplace manifest, however common in the repos scanned.
- No migration tooling for `packs/`.

**Recommended approach.** Extend `MANIFEST` with an optional destination column rather than inventing
a second config file — the format is already "one resource path per line" with `#` comments, and a
trailing map keeps a single source of truth for what gets deployed where. Default destination stays
the current one, so AC1 is satisfied by construction rather than by care.

Write the projection as new code in `lib/harness-fetch.sh` alongside `harness_copy_manifest`, not by
repurposing `install_abs` — its header comment forbids that, and `ADR-0001` deliberately moved the
base install off the symlink model.

Do the `AGENTS.md` correction **first**, in its own commit. It is a three-line edit that independently
closes half of the user's reported problem, and it is the piece most likely to be lost if the install
work runs long.

**On AC3, which is the one that actually matters.** Every other criterion can pass while a Codex
session still fails to find a skill — file layout is a proxy, not the goal. Do not substitute a
directory listing for a transcript. If Codex is not available in the environment, report AC3 as
**BLOCKED with the reason**, and do not mark the task Done: `DDR-0007`'s follow-up names this
explicitly — *"B2 must verify a real Codex session finds a skill by name, not merely that files
landed."*
